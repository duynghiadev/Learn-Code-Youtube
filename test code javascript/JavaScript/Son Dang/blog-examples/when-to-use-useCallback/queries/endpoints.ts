import { API_URL } from "common/constants";

// Can be loaded from .env but leave it here
export const TODOS = `${API_URL}`;
export const ADD_TODO = `${API_URL}/add-todo`;
export const UPDATE_TODO = `${API_URL}/update-todo`;
