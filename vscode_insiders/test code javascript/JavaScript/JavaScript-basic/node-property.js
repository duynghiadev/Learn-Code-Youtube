/*
 * HTML DOM có nghĩa là gì ?
 *
 * - HTML -> Hypertext Markdown Language
 * - DOM -> Document Object Model
 * - HTML DOM là một cơ chế để tạo ra các thành phần trong HTML.
 * - HTML DOM có thể được tạo ra bằng cách sử dụng các thành phần HTML.
 *
 * => DOM tổng thể gôm có 3 phần:
 *  + Element: ID, class, tag, attribute, style, event, CSS Selector
 *  + Attribute:
 *  + Text
 */

var boxElement = document.querySelector(".box");

console.log([boxElement]);
