// Modules, export default, named export trong ES6

const name = 'Jon Doe'
const age = 29
const printMessage = () => {
  console.log('I am a from file one')
}

export { name, age, printMessage } // name export
